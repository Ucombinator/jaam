
The visualizer must be built with sbt to receive messages using the Message class in Messaging.scala.

Running ‘sbt visualizer/assembly’ (from the jaam directory) produces visualizer.jar, which can be run with ‘java -jar visualizer.jar.’